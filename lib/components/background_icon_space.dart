import 'package:flutter/material.dart';

class BackgroundIconSpace extends StatelessWidget {
  const BackgroundIconSpace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.phone_android,
      color: Colors.white,
      size: 40,
    );
  }
}
